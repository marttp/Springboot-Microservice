server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marttp/config-server-spring-boot-microservice-training-1
          clone-on-start: true